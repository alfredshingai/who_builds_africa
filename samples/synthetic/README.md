# Synthetic Samples — DO NOT TREAT AS REAL

All records below are **SYNTHETIC** — fabricated to demonstrate schema, validation, and map rendering. They are not evidence and must never be published as real claims. Real data goes in `data/curated/` and must be source-backed.

Run `python scripts/validate.py --samples` to validate these without affecting curated data.

Countries: Zimbabwe (ZWE), Kenya (KEN), Ethiopia (ETH) — 5 synthetic projects, 7 orgs, 5 contracts, 3 sources.
