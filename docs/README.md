# Docs — MkDocs scaffold (Phase 0)

Will host methodology, dictionary, sourcing guide via GitHub Pages. For MVP, docs are Markdown in repo root (`DATA_METHODOLOGY.md`, etc.) — this folder will hold `mkdocs.yml` + site generation in Phase 2.

Deploy: `mkdocs gh-deploy` or Astro static copy to `docs/` for Pages.

Required map attribution page: OSM © + license link.
