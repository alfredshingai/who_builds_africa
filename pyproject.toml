[project]
name = "who-builds-africa"
version = "0.1.0"
description = "Africa Contract Footprints - validation and build pipelines"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7","frictionless>=5.17","pyyaml>=6.0","python-dateutil>=2.9"]

[project.optional-dependencies]
dev = ["pytest>=8.0","ruff>=0.5"]

[tool.ruff]
line-length = 100
